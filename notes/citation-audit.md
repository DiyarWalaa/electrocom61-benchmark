# Citation audit

Generated by `scripts/citation_audit.py`; run record in `runs/20260818_citation_audit_07/`. Regenerate after any change to the prose or the bibliography rather than editing this file.

**One row per citation USE, not per entry.** The same paper can support one sentence and not another, so each `\citep` instance is listed separately and a multi-key citation is expanded into one row per key. Citation commands are left inside the sentences: where in a sentence a citation sits is part of what needs checking.

This sheet records where each citation sits. It cannot record whether the citation is correct --- that is what reading the sentence beside the source is for.

## Counts

| | |
|---|---|
| citation instances | **82** |
| distinct entries cited | **36** |
| entries in `references.bib` | 38 |
| entries cited but never re-used (one instance) | 14 |
| entries cited in more than one place | **22** |
| entries in the bibliography with no citation | 2 (`dodge2020finetuning`, `graber2025addendum`) |

### Entries cited in more than one place

These need the most care: each use is a separate claim, and the sheet below lists them together so a paper's uses can be worked through in one pass.

| bib key | uses |
|---|---|
| `yolov12paper` | 10 |
| `yolov13paper` | 10 |
| `electrocom61` | 9 |
| `kong2026edge` | 3 |
| `akesson2024random` | 2 |
| `chen2023run` | 2 |
| `coco` | 2 |
| `detr` | 2 |
| `figueiredo2024leakage` | 2 |
| `graber2025resolving` | 2 |
| `gundersen2023reporting` | 2 |
| `gupta2019lvis` | 2 |
| `joeres2025datasail` | 2 |
| `kapoor2023leakage` | 2 |
| `lu2022sorting` | 2 |
| `picard2021seed` | 2 |
| `sharma2024vision` | 2 |
| `ultralytics` | 2 |
| `vasu2023mobileone` | 2 |
| `yolov10` | 2 |
| `yolov13` | 2 |
| `yolov13repo` | 2 |

## The three prior studies

PDFs held and already checked. These are the studies this paper argues with; every claim about them should be verifiable against a page.

| bib key | section | subsection | sentence |
|---|---|---|---|
| `electrocom61` | Introduction | (section opening) | ElectroCom61 is a public dataset for this task, containing 2,121 images of 61 component classes carrying 12,937 annotations, distributed with a fixed train/validation/test partition \citep{electrocom61}. |
| `electrocom61` | Related Work | ElectroCom61 and the results reported on it | ElectroCom61 contains 2,121 images of 61 electronic component classes carrying 12,937 annotations, photographed with several smartphones and distributed under CC BY 4.0 with a published partition of 1478/438/205 images \citep{electrocom61}. |
| `electrocom61` | Related Work | ElectroCom61 and the results reported on it | The dataset paper's own technical validation reports 95.9% mAP@50 for YOLOv9s \citep{electrocom61}; the two benchmark studies each compare several detectors and report mAP@50, both tabulating 84.9% for that same architecture \citep{yolov12paper,yolov13paper}. |
| `electrocom61` | Dataset Audit | One camera is never evaluated | This matters because the capture equipment was a design decision rather than an incidental one: three phones were used to ensure diverse imaging qualities \citep{electrocom61}. |
| `electrocom61` | Dataset Audit | One camera is never evaluated | The metadata records four distinct device identifiers — ONEPLUS\_NORD on 391 images, REDMI on 1,180, X on 311 and iPhone on 189, with a further 50 images carrying no metadata row — while the dataset paper describes three cameras, tabulating them as Smartphone 1, 2 and 3 and giving the third the display and cover-glass specifications of an iPhone \citep{electrocom61}. |
| `electrocom61` | A Corrected Split | What is released | The manifest is offered under CC BY 4.0, the licence of the dataset it derives from, and the attribution to the dataset authors travels with it \citep{electrocom61}. |
| `electrocom61` | Benchmark Setup | Efficiency measurement | Of the published studies reporting results on this dataset, the two that report parameter counts \citep{yolov12paper,yolov13paper} do not state whether those counts are fused or unfused, and the dataset paper \citep{electrocom61} reports none at all: its results table carries only mAP@50, precision, recall, F1 and total loss. |
| `electrocom61` | Discussion | Why published results on this dataset disagree | The archived notebook that produces the dataset paper's headline figure downloads Roboflow export version 5, while the archive published at the Mendeley DOI is version 9 \citep{electrocom61}; the two differ in image count and in the preprocessing applied before export, as Section \ref{sec:metadata-provenance} documents for the metadata that travels with them. |
| `electrocom61` | Data and Code Availability | (section opening) | The dataset itself is not redistributed: the manifest names images in a tree the reader downloads from the dataset's own archive \citep{electrocom61}, which is also what keeps the two licences separable. |
| `yolov12paper` | Introduction | (section opening) | Three published studies report detection results on it: the dataset paper's own technical validation and two subsequent benchmark studies \citep{yolov12paper,yolov13paper}. |
| `yolov12paper` | Related Work | ElectroCom61 and the results reported on it | The dataset paper's own technical validation reports 95.9% mAP@50 for YOLOv9s \citep{electrocom61}; the two benchmark studies each compare several detectors and report mAP@50, both tabulating 84.9% for that same architecture \citep{yolov12paper,yolov13paper}. |
| `yolov12paper` | Related Work | Electronic component detection and e-waste sorting | Both benchmark studies on this dataset are themselves e-waste detection systems, and both build on that family \citep{yolov12paper,yolov13paper}. |
| `yolov12paper` | Benchmark Setup | Efficiency measurement | Of the published studies reporting results on this dataset, the two that report parameter counts \citep{yolov12paper,yolov13paper} do not state whether those counts are fused or unfused, and the dataset paper \citep{electrocom61} reports none at all: its results table carries only mAP@50, precision, recall, F1 and total loss. |
| `yolov12paper` | Benchmark Setup | Models evaluated | The first three appear in the comparison tables of both prior studies of ElectroCom61 \citep{yolov12paper,yolov13paper}, and are included so that the present benchmark can be placed alongside theirs. |
| `yolov12paper` | Benchmark Setup | Training configuration | Both prior studies of this dataset state these values \citep{yolov12paper,yolov13paper}. |
| `yolov12paper` | Results | A published ordering not reproduced | One prior study of this dataset reports YOLOv12s as its most balanced performer, at 89.1% mAP@50 \citep{yolov12paper}. |
| `yolov12paper` | Discussion | Why published results on this dataset disagree | Neither benchmark study states which version it used \citep{yolov12paper,yolov13paper}. |
| `yolov12paper` | Discussion | Why published results on this dataset disagree | The earlier benchmark study does report it: each cell of its three accuracy columns carries a mean and standard deviation with a 95% confidence interval, and its text reports paired $t$-tests at $p < 0.05$ \citep{yolov12paper}. |
| `yolov12paper` | Discussion | Why published results on this dataset disagree | The two papers report the same seven rows with identical values, the second adding one row for its own architecture, and they share three authors — three of the four listed on the earlier paper are among the six on the later one \citep{yolov12paper,yolov13paper}. |
| `yolov13paper` | Introduction | (section opening) | Three published studies report detection results on it: the dataset paper's own technical validation and two subsequent benchmark studies \citep{yolov12paper,yolov13paper}. |
| `yolov13paper` | Related Work | ElectroCom61 and the results reported on it | The dataset paper's own technical validation reports 95.9% mAP@50 for YOLOv9s \citep{electrocom61}; the two benchmark studies each compare several detectors and report mAP@50, both tabulating 84.9% for that same architecture \citep{yolov12paper,yolov13paper}. |
| `yolov13paper` | Related Work | Electronic component detection and e-waste sorting | Both benchmark studies on this dataset are themselves e-waste detection systems, and both build on that family \citep{yolov12paper,yolov13paper}. |
| `yolov13paper` | Benchmark Setup | Efficiency measurement | Of the published studies reporting results on this dataset, the two that report parameter counts \citep{yolov12paper,yolov13paper} do not state whether those counts are fused or unfused, and the dataset paper \citep{electrocom61} reports none at all: its results table carries only mAP@50, precision, recall, F1 and total loss. |
| `yolov13paper` | Benchmark Setup | Models evaluated | The first three appear in the comparison tables of both prior studies of ElectroCom61 \citep{yolov12paper,yolov13paper}, and are included so that the present benchmark can be placed alongside theirs. |
| `yolov13paper` | Benchmark Setup | Training configuration | Both prior studies of this dataset state these values \citep{yolov12paper,yolov13paper}. |
| `yolov13paper` | Discussion | Why published results on this dataset disagree | Neither benchmark study states which version it used \citep{yolov12paper,yolov13paper}. |
| `yolov13paper` | Discussion | Why published results on this dataset disagree | Neither benchmark study names which partition its figures come from, and the later one states a 3-fold cross-validation over the whole dataset, so what partition any of its rows rests on is not established \citep{yolov13paper}. |
| `yolov13paper` | Discussion | Why published results on this dataset disagree | The later study reproduces the same table without the variance columns \citep{yolov13paper}. |
| `yolov13paper` | Discussion | Why published results on this dataset disagree | The two papers report the same seven rows with identical values, the second adding one row for its own architecture, and they share three authors — three of the four listed on the earlier paper are among the six on the later one \citep{yolov12paper,yolov13paper}. |

## Everything else

Registry-resolved but **not read from within this repository**, which is the state Section 8 concedes. Each row is a sentence that asserts something about the cited work.

| bib key | section | subsection | sentence |
|---|---|---|---|
| `akesson2024random` | Related Work | Metrics and efficiency reporting | Scanning many seeds on standard image benchmarks finds the spread between them modest, but seeds performing markedly better or worse than the average easy to come across \citep{picard2021seed}, and conclusions drawn from model comparisons with small effect sizes have been shown to be unreliable without repeated runs \citep{gundersen2023reporting}; where random effects during training have been studied directly, a statistically significant difference was found to be a weak indicator of a true one \citep{akesson2024random}. |
| `akesson2024random` | Limitations | (section opening) | Testing would not settle the question either: where random effects during training have been studied directly, a statistically significant difference has been reported to be a weak indicator that a true difference exists \citep{akesson2024random}. |
| `apicella2025leakage` | Related Work | Dataset splits, leakage and evaluation validity | Defects in how data is partitioned are documented across many scientific fields and have been identified as a contributor to irreproducible machine-learning results \citep{kapoor2023leakage}, and the ways they arise have been organised into taxonomies covering both conventional and transfer-learning settings \citep{apicella2025leakage}. |
| `bernett2024guiding` | Related Work | Dataset splits, leakage and evaluation validity | Where observations are not independent — repeated measurements of the same subject, samples from the same source — a split that ignores the grouping places related items on both sides of the boundary and inflates measured performance; this has been shown directly for connectome-based models \citep{rosenblatt2024leakage}, and, for biological domains, checklists exist for detecting the condition before a model is trained \citep{bernett2024guiding}. |
| `chen2023run` | Related Work | Metrics and efficiency reporting | A second concerns FLOPs alone: reducing them does not reduce latency proportionally, because latency is FLOPs divided by an achieved operations-per-second rate that falls for memory-bound operators \citep{chen2023run}. |
| `chen2023run` | Discussion | What accuracy alone cannot express | Across mobile networks, latency is only weakly correlated with parameter count and moderately correlated with FLOPs \citep{vasu2023mobileone}; reducing FLOPs does not reduce latency proportionally \citep{chen2023run}; and a benchmark of nine YOLO variants across three edge systems-on-chip finds latency tracking accuracy more closely than either quantity \citep{kong2026edge}. |
| `chen2025augmentation` | Related Work | ElectroCom61 and the results reported on it | That set was established by citation search on Google Scholar in August 2026: of the works citing the dataset paper, two are those benchmark studies and two cite the dataset as related work without training on it \citep{nguyen2025iotkits,chen2025augmentation}. |
| `coco` | Related Work | Metrics and efficiency reporting | The COCO benchmark established mAP averaged over ten intersection-over-union thresholds from 0.5 to 0.95 as the standard detection metric, alongside the single-threshold mAP@50 that preceded it \citep{coco}. |
| `coco` | Benchmark Setup | Evaluation protocol | Four accuracy quantities are reported. mAP@50 is included because it is the only accuracy metric the three prior studies tabulate, and comparability requires it. mAP@50--95, the COCO standard \citep{coco}, averages precision over ten intersection-over-union thresholds rather than one and is therefore sensitive to localisation quality as well as to detection; both benchmark studies plot it in their training curves, but neither tabulates a value for any model, so no published figure exists to compare against. |
| `detr` | Related Work | Metrics and efficiency reporting | Non-maximum suppression is a recognised postprocessing cost, and removing it is an explicit design goal in recent detectors — in the transformer line since DETR \citep{detr}, and within the YOLO family in architectures trained to produce a single prediction per object \citep{yolov10}. |
| `detr` | Benchmark Setup | A configuration that is uniform but not neutral | Transformer-based detectors, since DETR \citep{detr}, are commonly trained at learning rates one to two orders of magnitude below those used for convolutional detectors, and the value adopted here was drawn from studies that evaluated only YOLO variants. |
| `figueiredo2024leakage` | Related Work | Dataset splits, leakage and evaluation validity | The same structure appears in object detection: datasets built from video contain frames with high spatiotemporal correlation, and clustering them by image feature so that whole clusters are allocated together removes the leakage an image-level split creates \citep{figueiredo2024leakage}. |
| `figueiredo2024leakage` | A Corrected Split | The grouping rule | This is the group-aware splitting that leakage-avoiding tools apply in other domains \citep{joeres2025datasail}, and that has been applied to detection datasets built from video by moving clusters of visually correlated frames together rather than individual frames \citep{figueiredo2024leakage}. |
| `graber2025resolving` | Related Work | Dataset splits, leakage and evaluation validity | An established benchmark has been corrected on these grounds, and the correction moved the numbers: on a binding-affinity benchmark, previously reported performance fell against an improved split, by margins that differed between models \citep{graber2025resolving}. |
| `graber2025resolving` | Discussion | What generalises beyond this dataset | The familiar defect is leakage — a test image also seen in training is easy for the wrong reason — which inflates the scores measured against it \citep{kapoor2023leakage}, and corrections in that direction lower them, as when the split of a binding-affinity benchmark was corrected and previously reported performance fell against it \citep{graber2025resolving}. |
| `gundersen2023reporting` | Related Work | Metrics and efficiency reporting | Scanning many seeds on standard image benchmarks finds the spread between them modest, but seeds performing markedly better or worse than the average easy to come across \citep{picard2021seed}, and conclusions drawn from model comparisons with small effect sizes have been shown to be unreliable without repeated runs \citep{gundersen2023reporting}; where random effects during training have been studied directly, a statistically significant difference was found to be a weak indicator of a true one \citep{akesson2024random}. |
| `gundersen2023reporting` | Limitations | (section opening) | Seed scans on standard image benchmarks report the spread between seeds as modest while individual seeds well above and well below the average remain easy to find \citep{picard2021seed}, and a model comparison resting on differences this small is precisely the case in which conclusions have been shown not to survive the randomness they were drawn through \citep{gundersen2023reporting}. |
| `gupta2019lvis` | Related Work | Dataset splits, leakage and evaluation validity | The possibility has been acknowledged: a long-vocabulary detection benchmark notes that the categories reaching its validation and test partitions may be a strict subset of those in training, because it adopts an existing image-level partition and cannot guarantee otherwise \citep{gupta2019lvis}. |
| `gupta2019lvis` | Dataset Audit | Class coverage under the published split | Where a long tail is the concern, rarity is defined by how many training images a category appears in, and it arises from the natural frequency of categories in images \citep{gupta2019lvis}; the fifteen classes here are among the most heavily annotated in the dataset. |
| `joeres2025datasail` | Related Work | Dataset splits, leakage and evaluation validity | Leakage-reduced splitting has been formalised as a combinatorial optimisation over similarity between items and solved with clustering heuristics, so that group-aware partitions can be produced automatically rather than by hand \citep{joeres2025datasail}. |
| `joeres2025datasail` | A Corrected Split | The grouping rule | This is the group-aware splitting that leakage-avoiding tools apply in other domains \citep{joeres2025datasail}, and that has been applied to detection datasets built from video by moving clusters of visually correlated frames together rather than individual frames \citep{figueiredo2024leakage}. |
| `kapoor2023leakage` | Related Work | Dataset splits, leakage and evaluation validity | Defects in how data is partitioned are documented across many scientific fields and have been identified as a contributor to irreproducible machine-learning results \citep{kapoor2023leakage}, and the ways they arise have been organised into taxonomies covering both conventional and transfer-learning settings \citep{apicella2025leakage}. |
| `kapoor2023leakage` | Discussion | What generalises beyond this dataset | The familiar defect is leakage — a test image also seen in training is easy for the wrong reason — which inflates the scores measured against it \citep{kapoor2023leakage}, and corrections in that direction lower them, as when the split of a binding-affinity benchmark was corrected and previously reported performance fell against it \citep{graber2025resolving}. |
| `kong2026edge` | Related Work | Metrics and efficiency reporting | On low-compute edge hardware, latency has been reported to track detection accuracy more closely than either quantity \citep{kong2026edge}. |
| `kong2026edge` | Discussion | What accuracy alone cannot express | Across mobile networks, latency is only weakly correlated with parameter count and moderately correlated with FLOPs \citep{vasu2023mobileone}; reducing FLOPs does not reduce latency proportionally \citep{chen2023run}; and a benchmark of nine YOLO variants across three edge systems-on-chip finds latency tracking accuracy more closely than either quantity \citep{kong2026edge}. |
| `kong2026edge` | Limitations | (section opening) | A benchmark of nine YOLO variants across three edge systems-on-chip reports memory bandwidth governing robustness in that setting \citep{kong2026edge}. |
| `lu2022sorting` | Introduction | (section opening) | Automating the recognition step is the standing motivation for applying object detection to the problem, and systems have been built to sort components once they have been detached from a board \citep{lu2022sorting} and to identify them in place on the board itself \citep{sharma2024vision}. |
| `lu2022sorting` | Related Work | Electronic component detection and e-waste sorting | Components detached from waste boards have been sorted automatically on a conveyor \citep{lu2022sorting}, and identified in place on the board itself \citep{sharma2024vision}. |
| `nguyen2025iotkits` | Related Work | ElectroCom61 and the results reported on it | That set was established by citation search on Google Scholar in August 2026: of the works citing the dataset paper, two are those benchmark studies and two cite the dataset as related work without training on it \citep{nguyen2025iotkits,chen2025augmentation}. |
| `picard2021seed` | Related Work | Metrics and efficiency reporting | Scanning many seeds on standard image benchmarks finds the spread between them modest, but seeds performing markedly better or worse than the average easy to come across \citep{picard2021seed}, and conclusions drawn from model comparisons with small effect sizes have been shown to be unreliable without repeated runs \citep{gundersen2023reporting}; where random effects during training have been studied directly, a statistically significant difference was found to be a weak indicator of a true one \citep{akesson2024random}. |
| `picard2021seed` | Limitations | (section opening) | Seed scans on standard image benchmarks report the spread between seeds as modest while individual seeds well above and well below the average remain easy to find \citep{picard2021seed}, and a model comparison resting on differences this small is precisely the case in which conclusions have been shown not to survive the randomness they were drawn through \citep{gundersen2023reporting}. |
| `puttero2024disassembly` | Related Work | Electronic component detection and e-waste sorting | Two problems sit either side of component recognition: batteries must be found before disassembly, which has been done on X-ray images of whole appliances \citep{sterkens2021xray}, and the components must then be located precisely enough to be removed, which has been treated as a detection problem on board images \citep{puttero2024disassembly}. |
| `rajeev2025ewaste` | Related Work | Electronic component detection and e-waste sorting | Classification of e-waste more broadly has been approached with customisable YOLO variants \citep{rajeev2025ewaste} and with convolutional classifiers built for real-time operation \citep{sarswat2024realtime}. |
| `rosenblatt2024leakage` | Related Work | Dataset splits, leakage and evaluation validity | Where observations are not independent — repeated measurements of the same subject, samples from the same source — a split that ignores the grouping places related items on both sides of the boundary and inflates measured performance; this has been shown directly for connectome-based models \citep{rosenblatt2024leakage}, and, for biological domains, checklists exist for detecting the condition before a model is trained \citep{bernett2024guiding}. |
| `rtdetr` | Benchmark Setup | Models evaluated | Five detectors were evaluated: YOLOv9s \citep{yolov9}, YOLO11s \citep{yolo11}, YOLOv12s \citep{yolov12}, YOLO26s \citep{yolo26} and RT-DETR-l \citep{rtdetr}. |
| `sarswat2024realtime` | Related Work | Electronic component detection and e-waste sorting | Classification of e-waste more broadly has been approached with customisable YOLO variants \citep{rajeev2025ewaste} and with convolutional classifiers built for real-time operation \citep{sarswat2024realtime}. |
| `sharma2024vision` | Introduction | (section opening) | Automating the recognition step is the standing motivation for applying object detection to the problem, and systems have been built to sort components once they have been detached from a board \citep{lu2022sorting} and to identify them in place on the board itself \citep{sharma2024vision}. |
| `sharma2024vision` | Related Work | Electronic component detection and e-waste sorting | Components detached from waste boards have been sorted automatically on a conveyor \citep{lu2022sorting}, and identified in place on the board itself \citep{sharma2024vision}. |
| `sterkens2021xray` | Related Work | Electronic component detection and e-waste sorting | Two problems sit either side of component recognition: batteries must be found before disassembly, which has been done on X-ray images of whole appliances \citep{sterkens2021xray}, and the components must then be located precisely enough to be removed, which has been treated as a detection problem on board images \citep{puttero2024disassembly}. |
| `ultralytics` | Benchmark Setup | Training configuration | Neither states an Ultralytics version, which is the more consequential omission: the Ultralytics release \citep{ultralytics} determines the layer composition, detection head, loss function and augmentation defaults of every model it provides, so two studies may both report results for "YOLOv9s" while training architectures that differ. |
| `ultralytics` | Discussion | Why published results on this dataset disagree | Neither study states an Ultralytics version \citep{ultralytics}, which is what fixes the layer composition, detection head, loss function and default augmentation of every model distributed under a given name, so two studies can report results for the same architecture while training different networks. |
| `vasu2023mobileone` | Related Work | Metrics and efficiency reporting | Ranking several mobile-friendly networks deployed on a mobile device, one study finds latency only weakly correlated with parameter count, at a Spearman coefficient of 0.30 with $p = 0.18$, and moderately correlated with FLOPs, at 0.47 with $p = 0.03$ \citep{vasu2023mobileone}. |
| `vasu2023mobileone` | Discussion | What accuracy alone cannot express | Across mobile networks, latency is only weakly correlated with parameter count and moderately correlated with FLOPs \citep{vasu2023mobileone}; reducing FLOPs does not reduce latency proportionally \citep{chen2023run}; and a benchmark of nine YOLO variants across three edge systems-on-chip finds latency tracking accuracy more closely than either quantity \citep{kong2026edge}. |
| `yolo11` | Benchmark Setup | Models evaluated | Five detectors were evaluated: YOLOv9s \citep{yolov9}, YOLO11s \citep{yolo11}, YOLOv12s \citep{yolov12}, YOLO26s \citep{yolo26} and RT-DETR-l \citep{rtdetr}. |
| `yolo26` | Benchmark Setup | Models evaluated | Five detectors were evaluated: YOLOv9s \citep{yolov9}, YOLO11s \citep{yolo11}, YOLOv12s \citep{yolov12}, YOLO26s \citep{yolo26} and RT-DETR-l \citep{rtdetr}. |
| `yolov10` | Related Work | Metrics and efficiency reporting | Non-maximum suppression is a recognised postprocessing cost, and removing it is an explicit design goal in recent detectors — in the transformer line since DETR \citep{detr}, and within the YOLO family in architectures trained to produce a single prediction per object \citep{yolov10}. |
| `yolov10` | Results | Efficiency | The two architectures without non-maximum suppression carry the lowest postprocessing cost, which is the saving an NMS-free detection head is designed to produce \citep{yolov10}, and the size of that advantage should be stated carefully. |
| `yolov12` | Benchmark Setup | Models evaluated | Five detectors were evaluated: YOLOv9s \citep{yolov9}, YOLO11s \citep{yolo11}, YOLOv12s \citep{yolov12}, YOLO26s \citep{yolo26} and RT-DETR-l \citep{rtdetr}. |
| `yolov13` | Benchmark Setup | Models evaluated | YOLOv13 \citep{yolov13} was not evaluated. |
| `yolov13` | Discussion | Why published results on this dataset disagree | One architecture in the later comparison table is not provided by that package at all, but by a separate repository describing itself as based on it \citep{yolov13,yolov13repo}. |
| `yolov13repo` | Benchmark Setup | Models evaluated | It is not provided by the Ultralytics release used here but by a separate repository whose own documentation describes it as based on that package \citep{yolov13repo}, and installing that distribution would change the framework version under which every other model was trained. |
| `yolov13repo` | Discussion | Why published results on this dataset disagree | One architecture in the later comparison table is not provided by that package at all, but by a separate repository describing itself as based on it \citep{yolov13,yolov13repo}. |
| `yolov9` | Benchmark Setup | Models evaluated | Five detectors were evaluated: YOLOv9s \citep{yolov9}, YOLO11s \citep{yolo11}, YOLOv12s \citep{yolov12}, YOLO26s \citep{yolo26} and RT-DETR-l \citep{rtdetr}. |

