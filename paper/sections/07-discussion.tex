\section{Discussion}
\label{sec:discussion}

% Sources, by subsection:
%   7.1  reference/electrocom61-yolov9.ipynb (archived, cells 29 and 41);
%        README.md "Reference material"; data/config_provenance.csv;
%        Sections 3.1, 5.1, 5.5
%   7.2  Sections 3.1, 3.3, 5.4
%   7.3  Sections 3.2, 6.1
%
% 7.3's GRABER SENTENCE CORRECTED 2026-08-17, with the identical sentence in
% 2.3. It said "two groups independently corrected the split of a single
% binding-affinity benchmark". An addendum is not a second group -- same authors,
% same journal -- and the genuinely independent work is a preprint that is not in
% this bibliography. The claim was removed rather than sourced, and the
% graber2025addendum citation with it. Full record in references.bib above
% graber2025resolving.
%
% WHAT SURVIVES IS ALL 7.3 NEEDS: that correcting a split lowered previously
% reported scores, which is the direction-of-error contrast this paragraph is
% built on. The word "single" also went, since it was doing work only for the
% two-groups framing. Note the drop was not uniform in the source; 7.3 does not
% claim it was, and must not be made to.
%   7.4  Sections 6.4, 6.5; vasu2023mobileone, chen2023run, kong2026edge;
%        data/published_proxy_claims.csv
%
% CORRECTED 2026-08-17. 7.4 carried the same false co-citation as 2.4 --
% "parameter counts and FLOPs both correlate poorly" for vasu2023mobileone and
% chen2023run together. FasterNet makes no claim about parameter counts and
% reports no coefficients; MobileOne reports an asymmetry (0.30 for parameters,
% 0.47 for FLOPs). See 2.4's header for the full record.
%
% THE CLOSING SENTENCE IS NOW LOAD-BEARING, where it used to be filler. It read
% "Section 6.4 finds the same here", which under the old flattened claim meant
% nothing in particular. It now says 6.4 ORDERS THE TWO PROXIES THE SAME WAY --
% GFLOPs nearly right, parameter count not -- which is a real convergence
% between an ImageNet-classification result on mobile silicon and a detection
% result on a P100. Keep it as a statement of agreement; do NOT expand it into a
% restatement of 6.4's numbers, which is what got 7.4 cut to one paragraph.
%
% 7.4 CUT TO ONE PARAGRAPH, 2026-08-15. It ran to four and three of them
% restated 6.2, 6.4 and 6.5 rather than interpreting them: the 0.0023 middle-of-
% the-field spread, the 0.24 ms repeatability figure, and a closing that shared
% its construction with 6.5's. Section 7 argues; it does not recap.
%
% WHAT WAS KEPT is the only material here that Section 6 does not carry: the
% proxy-failure literature, which places 6.4's local finding in a documented
% pattern. WHAT WAS DROPPED, so it is not mistaken for a loss: the 0.0023 and
% 33.53 ms figures (6.2 and 6.4 state them), the 0.24 ms restatement (5.5 is now
% the single prose statement of it) and the 0.0001-mAP-against-7.37-ms
% construction, which stays in 6.5 alone.
%
% NOT FOLDED INTO 7.1, deliberately. 7.1's claim is that the three published
% ACCURACY figures are not comparable with one another -- a claim about
% reconciling a record. This is a different claim: that an accuracy-only
% comparison structurally cannot express deployment cost, whatever its
% reporting quality. Merging them would give 7.1 two claims and make both harder
% to check, and 8's and 9's closing arguments both lean on this one standing
% separately. The label is unreferenced, so keeping the subsection costs
% nothing.
%
% PENDING AUTHOR VERIFICATION AGAINST THE PRIOR-WORK PDFs. Three claims in 7.1
% rest on readings of papers that are NOT in this repository. Nothing in the
% tooling has checked them. Each is written narrowly, and each needs a page
% reference before submission:
%
%   1. THE FORK -- **REMOVED** from both 7.1 and 5.1 on 2026-08-14, rather than
%      left in place unsourced. That YOLOv13 is distributed as a fork outside
%      Ultralytics appears in NEITHER prior paper; its source would have to be
%      the iMoonLab repository or the YOLOv13 arXiv paper, neither in this
%      repository and neither consulted. It remains an OPEN SEARCH ITEM. 5.1 now
%      rests on the narrower claim that YOLOv13 is not among the architectures
%      the Ultralytics release used here provides, which is sufficient to
%      justify the exclusion; see 05's header for that claim's own verification
%      status, which is not perfect either.
%
%   2. THE CROSS-VALIDATION PROTOCOL -- pending verification, source located.
%      The YOLOv13 paper states 3-fold cross-validation over the entire dataset
%      at the END OF ITS SECTION III (methodology); the comparison table whose
%      rows are single-split figures is its TABLE I, in SECTION IV.
%      notes/writing-corrections.md records the reading. Confirm both locations
%      against the PDF before submission.
%
%   3. THE PUBLISHED COMPLEXITY FIGURES -- written firmly, on PARAMETERS ONLY.
%      Source: data/published_complexity.csv (both papers' Table I; YOLOv12
%      paper Sec. IV, YOLOv13 paper Sec. IV), set against measured values by
%      scripts/compare_published_complexity.py in
%      runs/20260814_compare_published_complexity/.
%
%      THE INPUT IS TYPED BY HAND FROM THE PDFs AND NOTHING IN THE TOOLING CAN
%      CHECK IT. A mis-keyed digit would propagate straight into a claim about
%      someone else's work. Every row carries a table_location column so each
%      value is re-checkable against its source in one step; do that before
%      submission. This is the only claim in the paper whose input is
%      unverifiable from within the repository AND whose conclusion is strong.
%
%      THE LAYER COLUMN IS DELIBERATELY NOT USED. It stays in the CSV, but no
%      conclusion is drawn from it: this study's own two layer conventions are
%      not interchangeable -- layers_modules counts nn.Module objects and is not
%      the figure Ultralytics prints, as collect_results.py states in terms and
%      warns against comparing to a published count. Two conventions that
%      disagree with each other cannot be used to convict a third.

Sections~\ref{sec:dataset-audit} to~\ref{sec:results} answer the question this
paper asks. A result computed on the published split averages over 45 of the 61
classes on validation and 46 on test; reported as a bare mAP@50 it rests on the
one of the two accuracy metrics whose ranking moves when the split changes; and
reported without a timing measurement it says nothing about whether the model is
fast enough to deploy. None of the three is a property of the detector, which is
why the disagreement in the published record was never about the models.

\subsection{Why published results on this dataset disagree}
\label{sec:disagreement}

At least four properties differ between the three published studies reporting
results on ElectroCom61 (Section~\ref{sec:related-electrocom61}), and none of
them states which value it used for any of the four. Each is enough on its own to move a reported figure by more than the
differences the comparisons are drawing conclusions from.

The first is the version of the dataset. The archived notebook that produces the
dataset paper's headline figure downloads Roboflow export version 5, while the
archive published at the Mendeley DOI is version 9 \citep{electrocom61}; the two
differ in image count and in the preprocessing applied before export, as
Section~\ref{sec:metadata-provenance} documents for the metadata that travels
with them. Neither benchmark study states which version it used
\citep{yolov12paper,yolov13paper}.

The second is the evaluation partition. In the archived notebook the evaluation
command is run without a test-split argument, so the reported figure is a
validation-set score; the test directory is never passed to an evaluation
command anywhere in it. Neither benchmark study names which partition its figures
come from, and the later one states a 3-fold cross-validation over the whole
dataset while reproducing the earlier study's central values unchanged, so what
partition any of its rows rests on is not established \citep{yolov13paper}. A
validation score and a test score are different quantities even when the split
is sound, and the notebook selects its best checkpoint on
the same partition it then reports.

The third is how many classes the figure averages over. Under the published
split any mAP averages 45 classes on validation and 46 on test rather than 61,
for the reasons established in Section~\ref{sec:class-coverage}. A figure
produced under a protocol that reaches all 61 classes is not the same quantity
as one produced under a protocol that reaches 46, and the fifteen classes that
differ between them are not a random sample --- they are among the most heavily
annotated in the dataset.

The fourth is the framework. Neither study states an Ultralytics version
\citep{ultralytics}, which is what fixes the layer composition, detection head,
loss function and default augmentation of every model distributed under a given
name, so two studies can report results for the same architecture while training
different networks. One architecture in the later comparison table is not
provided by that package at all, but by a separate repository describing itself
as based on it \citep{yolov13,yolov13repo}. That study names no package,
repository or version for that row or for any other, so what produced any of its
figures cannot be established from the paper.

The complexity columns are separable from the accuracy ones, because both
studies do state parameter counts, and those can be set beside measured values
directly. For the three architectures common to their tables and to this study,
the published counts are 0.26, 2.13 and 8.46 times the values measured here:
2.5~M against 9.44~M for YOLO11s, 19.7~M against 9.25~M for YOLOv12s, and
60.8~M against 7.19~M for YOLOv9s. The discrepancies run in both directions, so
no single systematic offset accounts for them. Fusing batch normalisation into
the preceding convolution --- the one convention difference these papers leave
unstated, and the obvious candidate --- moves a parameter count by at most 5.1\%
in this study's own measurements, and every published value here falls outside
the interval between the fused and unfused figures. Fusion therefore explains
none of them.

The same pattern appears in how variance is handled, and there it costs more.
The earlier benchmark study does report it: each cell of its three accuracy
columns carries a mean and standard deviation with a 95\% confidence interval,
and its text reports paired $t$-tests at $p < 0.05$ \citep{yolov12paper}. What it
does
does not state is how many runs those estimates were computed over, or by what
procedure --- and without the number of runs, a standard deviation cannot be
converted into anything a reader can check or reproduce. The later study
reproduces the same table without the variance columns \citep{yolov13paper}. The
later study, which prints no variance at all, is the one that states a procedure:
a 3-fold cross-validation with $k = 3$ and three validation runs, reported for
its own architecture. The estimates and the procedure are therefore attached to
different sets of figures, and neither study states the relationship. A
figure reported with an interval whose derivation is unstated is not more
checkable than one reported without it; it only appears to be.

The two papers report the same seven rows with identical values, the second
adding one row for its own architecture, and they share three authors --- three
of the four listed on the earlier paper are among the six on the later one
\citep{yolov12paper,yolov13paper}. Whatever the source of those seven figures,
they were not independently re-derived by the second study, so the published
record contains one measurement of them rather than two agreeing ones. That bears on how much evidence stands behind the numbers, and it is
stated here as a property of the record. What produced the discrepancy is not
established: a different variant, input resolution, class count or framework
release would each be sufficient, and none of the papers states which of those
it held fixed.

None of this establishes that any published figure is wrong. Each of the four is
a property that a reader needs in order to know what a number refers to, and
that the published record does not supply. The claim is that the figures are not
comparable with one another --- which is a question about reporting, and is
answerable by reporting.

\subsection{What would have caught it}
\label{sec:what-would-have-caught-it}

The coverage defect was invisible at the level at which splits are normally
checked. Aggregated over all 2121 images the published split reads 69.7 / 20.7 /
9.7, which is a textbook 70/20/10, and it reaches that figure through deviations
that cancel (Section~\ref{sec:allocation-uniformity}). A reader verifying the
split ratio would have found nothing.

Three checks would have found it, none of which requires training a model and
each of which costs minutes. Count the classes with no instances in the
evaluation partitions. Compute the allocation of each capture session
separately, rather than the aggregate. Read the number of classes the evaluation
framework actually scored.

The third is the cheapest and the most telling, because a framework that computes
mAP already has the number. Ultralytics reports that count on every evaluation it
runs, and on the published split it reads 45 on validation and 46 on test against
a dataset described throughout as having 61 classes. None of the three studies
reports the count, and none states which framework release produced its figures.
Reporting it beside the score --- as Section~\ref{sec:eval-protocol} does here
--- would have made
the defect self-evident without anyone looking for it.

\subsection{What generalises beyond this dataset}
\label{sec:generalisation}

Nothing about the defect is specific to electronic components. It arises
whenever data is collected in sessions and split by image: any class
photographed only within sessions that happen to fall entirely in training
becomes unevaluable, and a randomly drawn image-level split does not prevent
this, because randomness over images is not randomness over sessions. Medical
images grouped by patient, agricultural images by field, industrial images by
production run all have the same structure.

The audit that found it needed no pixels and no model. Filenames, label files
and the metadata shipped with the archive were sufficient to establish the
coverage gap, its mechanism and its extent. That matters for whether such a
check is worth running: it is not a research contribution, it is a few minutes
of arithmetic over files that any user of a dataset already has.

The transferable lesson is the direction of the error. The familiar defect is
leakage --- a test image also seen in training is easy for the wrong reason ---
which inflates the scores measured against it \citep{kapoor2023leakage}, and
corrections in that direction lower them, as when the split of a
binding-affinity benchmark was corrected and previously reported performance
fell against it
\citep{graber2025resolving}. Here the defect was exclusion
rather than leakage, and what it excluded was disproportionately easy, so it
\emph{deflated} the scores (Section~\ref{sec:accuracy}). ``A broken split means
inflated results'' is a useful heuristic and not a rule. A defect that
understates is the harder one to notice, because nobody audits a result for
being too low.

\subsection{What accuracy alone cannot express}
\label{sec:speed-changes-the-answer}

An accuracy-only comparison has no way to state deployment cost, and the
complexity figures usually offered in its place do not recover it. Whether a
model keeps up with a conveyor that does not stop for it is the question that
decides deployment, and no accuracy metric encodes it; the three prior studies
could not answer it because they measured a different thing
(Section~\ref{sec:deployment}). Substituting a proxy is not a repair, and the way it
fails is documented rather than local. Across mobile networks, latency is only
weakly correlated with parameter count and moderately correlated with FLOPs
\citep{vasu2023mobileone}; reducing FLOPs does not reduce latency
proportionally \citep{chen2023run}; and a benchmark of nine YOLO variants
across three edge systems-on-chip finds latency tracking accuracy more closely
than either quantity \citep{kong2026edge}. Section~\ref{sec:efficiency} orders
the two proxies the same way, on different hardware and a different task. What a deployment decision needs is therefore a measurement, not an
inference from architecture --- which is the one thing on this list that no
amount of care in reporting accuracy can supply.
